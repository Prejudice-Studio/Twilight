# 邮箱验证

邮箱模块包含绑定验证、找回密码、强制绑定、管理员审查和自动清理。

## 用户流程

- 绑定或更换邮箱时发送验证码。
- 通过邮箱找回密码。
- 开启登录通知后接收邮件提醒。

## 管理员流程

- 查看邮箱验证记录。
- 为用户绑定邮箱。
- 修改邮箱验证状态。
- 清理过期验证码和长期未验证邮箱。

## 安全规则

- SMTP 未配置时，发码、验证和找回密码接口必须返回明确错误。
- 黑名单、白名单域名规则必须在服务端校验。
- 验证码必须有有效期，并可被定期清理。
