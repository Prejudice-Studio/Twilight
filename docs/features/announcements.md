# 公告系统

公告可展示在仪表盘和独立公告页，支持置顶、折叠和长内容截断。

## 渲染模式

- `plain`：纯文本。
- `markdown`：安全 Markdown 子集。
- `bbcode`：常见论坛标签。

渲染由 `webui/src/lib/safe-render.tsx` 处理，包含 URL 白名单过滤。修改公告渲染时必须保留安全边界。

## 展示规则

- 置顶公告单独展示。
- 不可见公告不应出现在普通用户页面。
- 长公告应避免挤压仪表盘主要信息。
