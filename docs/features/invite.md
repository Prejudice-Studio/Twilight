# 邀请树

邀请系统记录用户之间的父子邀请关系，并提供邀请注册和下级续期能力。

## 用户能力

- 在功能开启且满足条件时生成邀请码。
- 查看自己生成的邀请码和邀请的下级。
- 为已有下级生成续期码。

## 管理员能力

- 查看完整邀请森林。
- 解绑过期下级。
- 对禁用、删除等操作配置级联深度。

## 禁用语义

`InviteEnabled=false` 时，新邀请入口必须拒绝请求。但已有邀请树的续期维护能力仍可保留，这是刻意设计。
