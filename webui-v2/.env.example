# Go API 地址，仅由 SSR 服务端读取；不要使用 NEXT_PUBLIC_ 前缀。
BACKEND_URL=http://127.0.0.1:5000

# adapter-node 监听配置；生产环境请由进程管理器注入真实值。
HOST=127.0.0.1
PORT=3001
ORIGIN=http://127.0.0.1:3001

# 如果后端修改了 session cookie 名称，在这里同步；不会复制其它上游 Cookie。
SESSION_COOKIE_NAME=twilight_session
